# Sample digest

This sample is sanitized and based on the mock fixture. It shows the shape of the Stage 1
output without exposing real workspace URLs, credentials, or private data.

```text
Incident digest - 3 job(s) with failures

## Error Test - ZeroDivisionError (job_id=101)
  [MEDIUM] [RECURRENT] main_task
    root cause: The job divides by zero in the main task, causing a deterministic Python
    ZeroDivisionError.
    action: fix (confidence 0.92, transient_vs_real=real, occurrences=2)

## Error Test - ImportError (job_id=103)
  [HIGH] [NEW] main_task
    root cause: The job imports a dependency that is not installed in the runtime environment.
    action: fix (confidence 0.88, transient_vs_real=real, occurrences=1)

## Nightly ETL - Large Join (job_id=111)
  [HIGH] [NEW] spark_task
    root cause: A large Spark join exhausted executor or driver memory and the run exceeded
    the configured timeout.
    action: escalate (confidence 0.81, transient_vs_real=evaluate, occurrences=1)
```

The real digest is generated by `python scripts/run_digest.py`. When validating against the
shared Databricks workspace, use a wider window if needed:

```bash
python scripts/run_digest.py --window "90 day"
```
